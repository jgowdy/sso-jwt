name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: godaddy/libenclaveapp/.github/workflows/reusable-ci.yml@main
    with:
      platforms: '["macos-latest", "ubuntu-latest", "windows-latest"]'
      linux_deps: "libdbus-1-dev pkg-config"
